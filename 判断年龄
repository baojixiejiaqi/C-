#include<stdio.h>
int main() {
	int age = 0;
	scanf(" %d",&age);
	if (age < 18 && age >0)
		printf("未成年\n");
	else if (age >= 18 && age <= 40)
		printf("青年\n");
	else if (age > 40 && age <= 150)
		printf("老年\n");
	else
		printf("请输入合理值");
	return 0;
}
